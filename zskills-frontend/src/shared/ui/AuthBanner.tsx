import Comment from "@/shared/ui/Comment";

export default function AuthBanner() {
  return (
    <div className=" h-full w-2/4 flex items-center justify-center bg-emerald-500 rounded-2xl overflow-hidden">
        <Comment mode="comment" />
    </div>
  );
}
